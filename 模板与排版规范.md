---
title: 模板与排版规范
tags:
  - 元文档
created: 2026-07-09
updated: 2026-07-09
---

# 模板与排版规范

> 本文件定义本仓库（Obsidian Vault）所有 Markdown 笔记的统一排版规范。新建笔记请复制本文件结构；旧笔记已按此规范批量整理。

## 1. 文件结构

每篇笔记由三部分组成：**Frontmatter（元信息）** → **H1 标题** → **正文**。

```markdown
---
title: 笔记标题
tags:
  - 分类标签
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# 笔记标题

> 一句话概述本篇笔记的核心内容。

## 1. 第一个大节

正文……

## 2. 第二个大节

### 2.1 子小节

……
```

## 2. Frontmatter 字段

- `title`：与文件名、H1 保持一致。
- `tags`：用父目录名作为主分类标签（如 `深度学习`、`git`、`MySQL`），可再补具体子标签。
- `created`：笔记创建日期，`YYYY-MM-DD`。
- `updated`：最近一次重大更新日期。

## 3. 标题层级

| 层级 | 语法 | 用途 | 示例 |
| --- | --- | --- | --- |
| H1 | `#` | 文档标题，**全篇唯一** | `# Transformer` |
| H2 | `##` | 主要章节，带阿拉伯编号 | `## 3. 整体架构` |
| H3 | `###` | 子章节，带二级编号 | `### 3.1 单词 Embedding` |
| H4 | `####` | 更细分点，少用，可不编号 | `#### 动态对齐` |

- 编号统一用 `1.` `2.`（半角点 + 空格），不再混用 `1、` `1.1、`。
- H1 与文件名一致；不要用 H1 当章节标题。

## 4. 强调与代码

- `**加粗**`：重要概念、术语首次出现。
- `==高亮==`：关键结论、需记忆要点（Obsidian 原生支持）。
- `` `行内代码` ``：命令、变量、文件名、标识符。
- 代码块必须标注语言：` ```python `、` ```sql `、` ```bash ` 等。
- 公式用 `$...$`（行内）或 `$$...$$`（块级）。

## 5. 图片规范

- 统一用 Markdown 语法：`![描述](相对路径)`，**不用** `<img>` HTML 标签。
- 路径用**相对路径**（相对当前 md 文件），正斜杠 `/`，不用反斜杠或绝对路径。
- 图片附件放在同级 `img/` 或 `image/` 目录下，按主题分子目录。
- 远程图床链接（如 jsDelivr CDN）保持原样即可。

```markdown
![Encoder 结构](img/encoder.png)
![动态图机制](image/day_01/动态图机制.webp)
```

## 6. 列表与缩进

- 无序列表用 `-`，有序列表用 `1.`。
- **不在段落首行加全角空格或 Tab 缩进**（Typora 旧习惯，Obsidian 不需要）。
- 列表嵌套用 4 空格缩进。

## 7. 目录与双链

- 仓库根目录 `README.md` 作为总索引（MOC），按分类链接到各笔记。
- 笔记间关联可用 Obsidian 双链 `[[文件名]]`，但与图床图片无关，按需使用。
